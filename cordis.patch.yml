# Bundle patch applied by the dsh host when this plugin is installed.
# Registers the crumbs plugin into the running cordis composition.
# (Standalone dev iteration still uses ./cordis.yml.)
- insert:
    - id: dsh-crumbs
      name: dsh-crumbs
